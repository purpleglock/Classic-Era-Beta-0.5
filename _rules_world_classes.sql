-- ════════════════════════════════════════════════════════════
-- ЛЕНТА ПРАВОК: природа мира, идеальные миры, исход кризиса
-- ────────────────────────────────────────────────────────────
-- ПОРЯДОК: после _rules_updates.sql и _world_classes.sql. Идемпотентно:
-- строка с тем же заголовком не дублируется при повторном накате.
-- ⚠️ Одно событие — одна строка (см. news-terse-one-event-one-line): три
-- правки = три записи, а не одна свалка и не десять мелких.
-- ════════════════════════════════════════════════════════════
do $$
declare v record;
begin
  for v in select * from (values
    ('Природа мира: класс планеты теперь считается',
     'Класс планеты множит четыре числа колонии: население (потолок и прирост), выработку рабочих (залежи, фабрики, торговля), науку и цену стройки. Множители видно в карточке колонии. Плохих классов нет — есть специализация: лава и пояса дают руду при тяжёлой демографии, аномалии и экзотика — науку, землеподобные и океанические — людей. Отрицательный прирост климат не ускоряет.',
     'gb-colonies', 'Колонии'),
    ('Идеальные миры',
     'На месте миров, переплавленных кризисом «Престол», остались планеты нового класса. Дебафов у них нет вовсе, а по каждой оси они заметно выше лучшего обычного класса: +50% к населению, +45% к рабочим и науке, −25% к цене стройки. Прежних владельцев и построек на них не осталось — это чистый лист на восемь ячеек, свободный для колонизации кем угодно. Больше таких миров не появится.',
     'gb-colonies', 'Колонии'),
    ('Кризис «Престол» ушёл к Храму мироздания',
     'Оно снялось со всех систем разом: огонь прекращён, оккупационные порядки сняты, войны закрыты без условий. Начатые переплавки доведены до конца перед уходом. Вся колонна — ковчег и крылья — идёт одним курсом к системе «Храм мироздания» и враждебных действий не ведёт. На картах систем, где стояли его якоря, вместо светила теперь чёрная звезда.',
     'gb-map', 'Кризис')
  ) as t(title, body, anchor, tag) loop
    if not exists(select 1 from public.rules_updates r where r.title = v.title) then
      insert into public.rules_updates(title, body, anchor, tag)
        values (v.title, v.body, v.anchor, v.tag);
    end if;
  end loop;
end$$;

# План работ по хронике

Собран из разбора 17.08. Конструктор — `lore/saga_constructor.md`, канон —
`lore/precursor_memory.md`.

Порядок неслучаен: **сперва проверить схему на одной главе, потом городить
движок и переписывать всё остальное.** Дважды написанный и дважды забракованный
текст — достаточная причина не писать триста абзацев вперёд.

---

## Сделано (17.08)

| | что | где |
|---|---|---|
| ✔ | Сроки сняты с узлов чтения; остались два по часу | `_precursor_saga_nowait.sql` **накатан** |
| ✔ | Найдена и снята причина: сроки в двух слоях, мир перебивал `'*'` (68 ч у Кайлата) | там же |
| ✔ | Досье Фонда убрано, миры показываются только хроникой | `precursor_ui.js` |
| ✔ | Печать по буквам удалена вместе с кольцом `paint → type → paint` | `precursor_saga.js` |
| ✔ | Панель примерки «Фон · Персонаж · Эмбиент» удалена | `precursor_saga.js` |
| ✔ | CSS-анимации и переходы удалены все | `css/39_precursor_saga.css` |
| ✔ | Конструктор: 6 полок, голос из 7 правил | `lore/saga_constructor.md` |

Врезанные развилки (Кайлат 7 → 15, Кхарн 11 → 14) при переписывании
**пойдут под нож** — они лечили симптом, а не болезнь. Оставлены, чтобы до
переписывания хроника была хоть проходимой.

---

## ⚠ Порядок пересмотрен (17.08)

Этапы 1 и 5 были написаны про **рукописи** (главы Кайлата), а работа — в
полках: миры, которые игрок находит, собирает `saga_layers.js`, и до правки он
выдавал всем одну и ту же сцену с подменёнными существительными. Поэтому
сделано сразу:

| | что | где |
|---|---|---|
| ✔ | Замер текста скриптом, а не на глаз (этап 5.3) | `tools/saga_lint.js` |
| ✔ | Полки: темы, люди с нуждой, ружья, события, цена (этап 4) | `saga_kit.js` |
| ✔ | `SagaLayers.build` пересажен на сборщик, скелет — запасной (4.5) | `saga_layers.js` |
| ✔ | Прогон графа и сверка, что два мира дают разные сцены | `tools/pc_kit_probe.js` |
| ✔ | Кайлат переписан под карточный голос (отрицаний 77→56%, длинных фраз 65→1%) | `saga_kailat.js` |

Замер собранных миров после чистки:

| | было (скелет) | стало |
|---|---|---|
| отрицание в кадре | 52–55% | **19 / 27 / 27%** (порог 25) |
| предложение > 140 зн. | — | **0%** |
| предложений в кадре > 4 | — | **0%** |
| запятых > 2 | — | 5–6% (перечни: «первая, вторая, третья…») |

Остаток отрицаний — реплики, где отрицание и есть содержание: «восьмого нет»,
«наши не продавали», «этого у нас нет и взять негде». Дальше резать — резать
смысл.

Попутно найдено замером и исправлено: `undefined` вместо беды в двух кадрах,
ружьё, посаженное дважды подряд, склейка «ставят мёртвых, и разбирают на общее».

⚠ 17.08, второй разбор: правка рукописей — та же ошибка, что и с этапами 1 и 5.
Работа всегда в полках. Файлы рукописей удалены, замер и план приведены к сборке.

Осталось: цена пока **объявлена в данных** (`cost` на варианте, ценник в тексте
до нажатия) — списывает её этап 2. Кхарн как рукопись не переписан намеренно:
редкий вариант, его текст до найденных миров не доезжает.

---

## Этап 1 · Проба схемы на одной главе

Цель — выяснить, работает ли конструктор, **до** больших вложений.

| № | Задача | Файл | Зависит |
|---|---|---|---|
| 1.1 | Заглушка цены: `pcgCost()` считает и показывает ценник, ничего не списывая | `precursor_saga.js` | — |
| 1.2 | Заглушка чаши: накопитель в состоянии хроники, без БД | `precursor_saga.js` | — |
| 1.3 | Проверка склада: вариант без ресурса **не рисуется** | `precursor_saga.js` | 1.1 |
| 1.4 | Реестр ружей Кайлата: 4 ружья с замками | `saga_kit.js` (новый) | — |
| 1.5 | **Глава I целиком по конструктору**: 6 фактов → 6 кадров, 3 варианта с ценой, 1 скрытый | `saga_kailat.js` | 1.1–1.4 |
| 1.6 | Прогон на стенде + показ | `_saga_probe.html` | 1.5 |

**Контрольная точка. Дальше не идём, пока глава не принята.**

---

## Этап 2 · Движок: цена и чаша

Начинается только после приёмки этапа 1.

| № | Задача | Файл |
|---|---|---|
| 2.1 | `p_cost` в `precursor_saga_step`: списывать груз и занимать время **при переходе** | `_precursor_saga_cost.sql` |
| 2.2 | Отказ при нехватке + возврат списанного в ответе | там же |
| 2.3 | Чаша как поле хроники; исход считается **по сумме решений**, а не по флагам последнего узла | `_precursor_saga_scale.sql` |
| 2.4 | Пересадка `FATES` на чашу | `saga_weave.js` |
| 2.5 | Снять заглушки 1.1–1.3, включить настоящий счёт | `precursor_saga.js` |

⚠ Порядок накатов: `cost` → `scale`. Оба идемпотентные.

---

## Этап 3 · Чаша в кадре

| № | Задача | Файл |
|---|---|---|
| 3.1 | Полоска узлов по низу сцены: число меток = настоящее число узлов мира | `precursor_saga.js`, `css/39_precursor_saga.css` |
| 3.2 | Действие узла по среде: огни у воды, тепло в камне, колодцы в суши | `saga_weave.js` (`ENVS`) |
| 3.3 | Метка меняется **только** после решения, в том же кадре | `precursor_saga.js` |
| 3.4 | Та же полоска на двери хроники в разделе | `precursor_saga.js` |

Запрет: ни чисел, ни подписей, ни тултипа. Метка — место, а не счётчик.

---

## Этап 4 · Полки конструктора в код

| № | Задача | Файл |
|---|---|---|
| 4.1 | Каталог событий по крючкам канона, 3–5 на крючок | `saga_kit.js` |
| 4.2 | Люди: к ролям поля `нужда · помеха · скрывает` | `saga_kit.js` |
| 4.3 | Темы со ступенями ставки | `saga_kit.js` |
| 4.4 | Сборщик главы «от решения назад» | `saga_kit.js` |
| 4.5 | `SagaLayers.build` пересажен на сборщик | `saga_layers.js` |

После этого **найденные игроком миры** тоже собираются по конструктору, а не
только две рукописи.

---

## Этап 5 · Текст ⟵ ПЕРЕПИСАН 17.08

**Рукописей больше нет.** `saga_kailat.js` и `saga_kharn.js` удалены с диска:
игрок видит только то, что собрали полки, а две рукописи третий раз утаскивали
работу в текст, до которого он не доезжает. Всё, что от них нужно, уже снято
в редкие варианты (`saga_layers2.js`).

| № | Задача | Где | Статус |
|---|---|---|---|
| 5.1 | Имя главы от события, а не от номера | `saga_kit.js`, `saga_layers2.js` | ✔ |
| 5.2 | Отрицания сборки ≤25% в каждой главе | `saga_kit.js` | II–III и пролог ещё над порогом |
| 5.3 | Замер по образцам сборки, рукописи из мерки убраны | `tools/saga_lint.js` | ✔ |

Замер — скриптом, а не на глаз. Пороги: отрицаний ≤25%, кадр 2–4 коротких
предложения, оговорка одна на главу. Остаток отрицаний в главах II–III —
реплики, где отрицание и есть содержание («восьмого нет», «взять негде»):
резать их дальше значит резать смысл, и мерку тут решает не скрипт.

---

## Этап 6 · Арты ⟵ нужен ты

```
assets/precursor/saga/    → пусто (только manifest.json)
assets/precursor/worlds/  → пусто (только manifest.json)
```

**Ноль изображений.** Движок ищет кадр по четырём путям и падает в ровный тон.
Без этого хроника останется текстом на заливке, как её ни напиши.

| № | Задача | Кто |
|---|---|---|
| 6.1 | Кадры глав: по одному на главу × 5 глав × 2 мира | ты |
| 6.2 | Спрайты названных людей: 3–4 на мир | ты |
| 6.3 | Сетка «раса × эпоха» — запасной кадр для найденных миров | ты |
| 6.4 | Заливка и проверка путей | я |

Правку кода это не требует: движок уже ищет `saga/<мир>/bg_<узел>.webp`,
кадр главы, потом сетку. Довольно положить файлы.

---

## Что нужно от тебя, чтобы начать

1. **Огни или лодки** — какое действие узла берём метафорой чаши (этап 3.2).
2. **Приёмка главы I** после этапа 1 — это единственная развилка всего плана.

Всё остальное решено и записано в конструкторе.
